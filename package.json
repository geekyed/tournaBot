{
  "name": "tournabotcommand",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rm -f tournaBotCommand.zip",
    "deploy": "yarn zip && aws lambda update-function-code --function-name tournaBotCommand --zip-file fileb://./tournaBotCommand.zip",
    "zip": "yarn clean && bestzip tournaBotCommand.zip ./*"
  },
  "dependencies": {
    "aws-sdk": "^2.351.0",
    "bestzip": "^2.1.2"
  }
}
